name: Send Alerts

on:
  schedule:
    - cron: "5 * * * *"
  workflow_dispatch:

jobs:
  alerts:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - run: pip install -r requirements.txt

      - run: python src/send_alerts.py
